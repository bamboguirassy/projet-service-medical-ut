import {BamboAbstractChartModel} from '../../shared/classes/bambo-abstract-chart-model';

export class ConsMensStats extends BamboAbstractChartModel{
    pats: string;
    per: string;
    famille: string;
    etudiant: string;
    month: string;
}
